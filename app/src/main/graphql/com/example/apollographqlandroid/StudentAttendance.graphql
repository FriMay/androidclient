query studentAttendance($groupSubjectId:Int!, $studentId:Int!){
    studentAttendance(studentId: $studentId, groupSubjectId: $groupSubjectId){
        stringData
        mark{
            markValue
        }
    }
}